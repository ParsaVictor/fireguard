# بازرسی دیتاست — نتایج اندازه‌گیری واقعی

تاریخ: ۲۰۲۶-۰۹-۰۴ · همهٔ اعداد این سند **اندازه‌گیری شده‌اند**، نه نقل‌قول از مقاله

---

## ۰ · تصمیم‌های ثبت‌شده در این مرحله

| تصمیم | وضعیت |
|---|---|
| قطعه‌بندی پیکسلی (SAM 2 / yolo26-seg) | ❌ **حذف شد** — کادر مستطیلی کافی است |
| تنظیمات پیچیده | ❌ حذف شد — یک سلول تنظیمات، بقیه خودکار |
| Pyro-SDIS / FIgLib / FLAME | ❌ حذف شدند — دامنهٔ اشتباه (جنگل/برج/پهپاد) |
| **FASDD_CV + D-Fire** | ✅ همین دو، ادغام‌شده |
| قرارداد کلاس | ✅ **`0 = fire` · `1 = smoke`** |

---

## ۱ · جواب سؤالت: آیا هر دو دیتاست، هم آتش و هم دود دارند؟

**بله — هر دو، و متعادل.**

### FASDD_CV

از خواندن **کاملِ** فهرست ۱۹۰,۲۵۷ ورودیِ zip (اصلاح‌شده — نسخهٔ اول این سند روی
پنجرهٔ ناقص حساب شده بود، بخش ۱۱ را ببین):

| دستهٔ فایل | تعداد | سهم | train | valid | test |
|---|---|---|---|---|---|
| `neitherFireNorSmoke_CV…` (منفی) | **۳۹,۱۱۴** | ۴۱.۱٪ | ۳۱,۳۷۳ | ۳,۸۶۰ | ۳,۸۸۱ |
| `smoke_CV…` (فقط دود) | **۲۳,۳۵۰** | ۲۴.۵٪ | ۱۸,۶۹۹ | ۲,۳۳۴ | ۲,۳۱۷ |
| `bothFireAndSmoke_CV…` (هر دو) | **۲۰,۱۲۶** | ۲۱.۲٪ | ۱۶,۰۶۳ | ۲,۰۴۵ | ۲,۰۱۸ |
| `fire_CV…` (فقط آتش) | **۱۲,۵۳۶** | ۱۳.۲٪ | ۹,۹۶۶ | ۱,۲۷۴ | ۱,۲۹۶ |
| **جمع** | **۹۵,۱۲۶** | | ۷۶,۱۰۱ | ۹,۵۱۳ | ۹,۵۱۲ |

← تصاویر دارای **آتش** = ۱۲,۵۳۶ + ۲۰,۱۲۶ = **۳۲,۶۶۲**
← تصاویر دارای **دود** = ۲۳,۳۵۰ + ۲۰,۱۲۶ = **۴۳,۴۷۶**

و جفت‌شدن تصویر/برچسب **کاملاً ۱:۱** است — در هر سه split صفر برچسبِ بی‌تصویر و صفر
تصویرِ بی‌برچسب.

### D-Fire

| دسته | تعداد |
|---|---|
| فقط آتش | ۱,۱۶۴ |
| فقط دود | ۵,۸۶۷ |
| هر دو | ۴,۶۵۸ |
| منفی | ۹,۸۳۸ |
| **کادر آتش / کادر دود** | ۱۴,۶۹۲ / ۱۱,۸۶۵ |

آینهٔ HuggingFace: `train` ۱۷,۲۲۱ + `test` ۴,۳۰۶ = ۲۱,۵۲۷ ✓ مطابق مقاله

### مجموع بعد از ادغام

```
~۱۱۳ هزار تصویر  ·  ~۳۷ هزار دارای آتش  ·  ~۵۳ هزار دارای دود  ·  ~۴۸ هزار منفی
```

نسبت منفی به مثبت ≈ ۱:۱.۴ — این دقیقاً چیزی است که آلارم کاذب را پایین می‌آورد.

---

## ۲ · ★ تلهٔ شمارهٔ ۱ — نگاشت کلاس **برعکس** است

این مهم‌ترین چیزی است که در این بازرسی پیدا شد.

| دیتاست | فایل `data.yaml` / قرارداد | کلاس ۰ | کلاس ۱ |
|---|---|---|---|
| **FASDD_CV** | `names: ['fire','smoke']` | 🔥 fire | 💨 smoke |
| **D-Fire** | — | 💨 **smoke** | 🔥 **fire** |

### چطور اثبات شد

`data.yaml` را با یک درخواست Range مستقیم از داخل zip بیرون کشیدم:

```yaml
nc: 2
names: ['fire', 'smoke']
roboflow:
  workspace: forestfiresmoke
  project: fasdd_cv-dx83j
  license: CC BY 4.0
```

و برای D-Fire، برچسب `AoF01166.txt` این بود:

```
0 0.5144531250000001 0.3527777777777778 0.89765625 0.525
```

تصویرش را دانلود کردم و کادر را رویش کشیدم: یک **تپه با مه دود** از `Camera0002`،
تاریخ 08.Oct.2020. کلاس ۰ روی دود نشسته بود. سپس روی ۱۳ نمونهٔ دیگر تکرار شد:
ناحیهٔ مه‌آلود همیشه کلاس ۰، نقاط شعلهٔ روشن همیشه کلاس ۱.

### اگر حل نشود

هر آتشی «دود» برچسب می‌خورد و هر دودی «آتش». مدل با mAP ظاهراً قابل قبول آموزش می‌بیند
(چون درونش سازگار است) ولی **در عمل هر آلارم برعکس است**. و چون هیچ خطایی نمی‌دهد،
ممکن است هفته‌ها متوجه نشوی.

**حل شد:** نوت‌بوک برچسب‌های D-Fire را با `1 - c` جابه‌جا می‌کند. روی نمونه‌ها آزمایش شد:
`۶۰ کادر جابه‌جا شد ۰↔۱`.

---

## ۳ · ★ تلهٔ شمارهٔ ۲ — فریم‌های پشت‌سرهم در D-Fire

نمونه‌گیری تصادفی از برچسب‌های ناخالی D-Fire، ۱۱ تصویر از **یک دوربین ثابت و یک شب**
بیرون داد: `Camera0001`، ۱۷.Oct.2019، از ۱۸:۴۳:۰۷ تا ۱۹:۱۹:۲۷.

شباهت `dHash` بین فریم‌های مجاور:

| جفت | شباهت |
|---|---|
| AoF04026 ↔ AoF04031 | **۸۹.۵٪** |
| AoF04058 ↔ AoF04061 | ۸۷.۵٪ |
| AoF04043 ↔ AoF04044 | ۸۷.۱٪ |
| AoF04032 ↔ AoF04034 | ۸۶.۳٪ |
| AoF04023 ↔ AoF04026 | ۸۳.۶٪ |

۹ جفت از ۷۸ جفت بالای ۸۰٪ شبیه‌اند. اینها فریم‌های یک ویدیو هستند، نه تصاویر مستقل.

**اگر تصادفی تقسیم شوند:** فریم ۱۲ در `train` و فریم ۱۳ در `test` می‌افتد. مدل عملاً
همان تصویر را در آزمون می‌بیند → **mAP دروغینِ بالا**. بعد در دنیای واقعی می‌ریزد و
نمی‌فهمی چرا.

**حل شد:** تقسیم گروه‌آگاه — کلید گروه از پیشوند نام ساخته می‌شود (`AoF04031` → `AoF040`)
و همهٔ اعضای یک گروه اجباراً در یک split می‌مانند.

آزمون روی نمونه‌ها:
```
تقسیم گروه‌آگاه روی 25 گروه
groups: 25 | groups straddling >1 split: 0
AoF040 members: 10 -> splits: {'train'}     ← هر ۱۰ فریم با هم ماندند ✓
```

---

## ۴ · هم‌پوشانی احتمالی بین دو دیتاست

مقالهٔ FASDD می‌گوید منابعش شامل «اینترنت، شبکه‌های اجتماعی، و **دیتاست‌های آتش متن‌باز**»
است. D-Fire (۲۰۲۲) یکی از همان دیتاست‌های متن‌باز است و FASDD (۲۰۲۳-۲۴) بعد از آن آمده.
یعنی **احتمال جدی دارد که بخشی از D-Fire داخل FASDD باشد**.

بدون دانلود کامل نمی‌شود قطعی گفت، پس نوت‌بوک حذف تکراریِ **بین‌دیتاستی** انجام می‌دهد:
`dHash` ۶۴ بیتی + جست‌وجوی چندشاخصی (دو تصویر با فاصلهٔ همینگ ≤۳ حتماً در یکی از چهار
تکهٔ ۱۶ بیتی مشترک‌اند → بدون مقایسهٔ ۶.۷ میلیارد جفتی).

آزمون: یک تصویر D-Fire را عمداً با نام FASDD کپی کردم →
`✅ تکراری‌ها: 1 حذف شد` و گزارش داد که جفت بین دو دیتاست مختلف بوده ✓

---

## ۵ · بهداشت داخلی FASDD — تمیز است

بررسی کردم که آیا صادرات Roboflow نسخه‌های augment‌شده ساخته (که باعث نشت می‌شود):

```
unique source ids : 95,126
total image files : 95,126
ids appearing >1x : 0
*** ids in >1 split (LEAKAGE) : 0 ***
```

**صفر تکراری، صفر نشت بین split ها.** خطِ «۲ نسخه از هر تصویر» در
`README.roboflow.txt` یک متن قالبی بوده، نه واقعیت.

---

## ۶ · کیفیت برچسب — آنچه با چشم دیدم

۲۲ نمونهٔ FASDD و ۱۳ نمونهٔ D-Fire را با کادرهایشان رسم کردم
(`dataset_samples/_preview/`).

### خوب ✅

- کادرها روی شیء درست نشسته‌اند
- **منفی‌ها واقعاً سخت‌اند**: غروب آفتاب با ابر نارنجی، ابرهای سفید، اتاق خالی داخلی،
  و **جلیقه‌های نجات قرمز** — دقیقاً منفی‌هایی که مدل را گول می‌زنند
- **صحنه‌های داخلی هست**: آتش دیده‌شده از پنجرهٔ ساختمان، کارخانهٔ داخلی با
  **دوربین فیش‌آی سقفی**، اتاق داخلی
- دوربین نظارتی واقعی با مهر زمان (遵义市 — شهر زون‌یی چین، ۲۰۱۷)
- منفی‌ها فایل برچسب **خالی** دارند — فرمت درست برای YOLO

### مشکل‌دار ⚠️

| مشکل | نمونه | چرا مهم است |
|---|---|---|
| **شمع و کبریت `fire` برچسب خورده** | `fire_CV008157` (شمع روی میز)، `fire_CV000916` (۴ شمع)، `fire_CV005973` (کبریت) | برای یک محصول ایمنیِ داخلی، **این یعنی آلارم روی هر شمع و فندک**. اولین جایی که اگر FAR بالا رفت باید نگاه کنی. |
| واترمارک عکس استاک | `fire_CV008157` → «alamy stock photo» | مدل ممکن است واترمارک را سرنخ بگیرد |
| تصاویر تقریباً سیاه | `fire_CV004472`، `fire_CV005721` | برش بد یا نوردهی خراب |
| بخشی از دودها، جنگلی و دوردست است | `smoke_CV021207` و مشابه | دامنهٔ ما نیست، ولی ضرر هم ندارد |

### دربارهٔ شمع — پیشنهاد

فعلاً دست نزن و **بگذار مدل آموزش ببیند**. بعد از فاز ۰، وقتی FAR واقعی را روی ویدیوی
خودت اندازه گرفتی، اگر شمع/فندک مشکل‌ساز شد دو راه داری:

1. حذف تصاویر «شعلهٔ کنترل‌شدهٔ کوچک» از آموزش (~چند صد تصویر)
2. بهتر: نگهشان دار، و در **لایهٔ تصمیم** (همان `HazardEngine` خودت) با اندازه و
   پایداری تفکیکشان کن — دقیقاً همان چیزی که با حالت `SMOKING` شهودی حدس زده بودی

---

## ۷ · چه چیزی الان روی سیستمت هست

```
آتش/dataset_samples/
├── FASDD_CV/          ۲۲ تصویر + برچسب  (fire · smoke · both · neither)
├── D-Fire/            ۱۳ تصویر + برچسب  (دوربین نظارتی شبانه)
└── _preview/
    ├── FASDD_CV_boxes.jpg    کادرها رسم‌شده
    └── D-Fire_boxes.jpg      کادرها رسم‌شده — توجه به برعکس بودن کلاس‌ها
```

نمونه‌های FASDD مستقیماً با درخواست Range از داخل zip ۳.۴ گیگی بیرون کشیده شدند —
بدون دانلود کل فایل.

---

## ۸ · چه چیزی در گوگل درایو هست

پوشه: **FireGuard_Datasets**
https://drive.google.com/drive/folders/1Yp6r11Hw5aL0H15zzgLl4E6Qk1RX2Uji

الان: فقط `README`.
بعد از اجرای نوت‌بوک: `FASDD_CV.zip` · `D-Fire.zip` · `fireguard_data.zip`

### چرا خودم آپلود نکردم

ابزار گوگل درایوی که در اختیار دارم فایل را به‌صورت base64 **از داخل همین گفت‌وگو**
می‌فرستد. برای ۳.۴ گیگابایت این عملاً ناممکن است.

راه درست هم همین نیست: **Colab اتصال بسیار سریع‌تری به HuggingFace دارد و درایو را
مستقیم mount می‌کند.** دانلود ۳.۴ گیگ آنجا ۵ تا ۱۵ دقیقه است، در حالی که از مسیر
سیستم تو ساعت‌ها طول می‌کشد و ۳۰ گیگ فضای آزادت را هم می‌خورد
(دیسک `C:` الان ۹۴٪ پر است — ۳۰ گیگ آزاد).

---

## ۹ · وضعیت آزمون نوت‌بوک

منطق ادغام/حذف‌تکراری/تقسیم را روی ۳۵ نمونهٔ واقعی اجرا کردم، نه فقط نوشتم:

```
MERGE : FASDD 23 تصویر (بدون تغییر) · D-Fire 13 تصویر (60 کادر جابه‌جا ۰↔۱) 🔄
DEDUP : 1 تکراری حذف شد — و تشخیص داد که بین دو دیتاست مختلف بوده ✓
SPLIT : 25 گروه · groups straddling >1 split: 0 ✓ · AoF040 هر ۱۰ فریم در train ✓
```

یک باگ در همین آزمون پیدا و رفع شد: سلول حذف‌تکراری به هر رکورد یک `dupgroup`
یکتا می‌داد که گروه‌بندی رویدادی را نابود می‌کرد (۳۵ گروه برای ۳۵ تصویر).
اصلاح شد به ادغام کلیدهای گروه با union-find → ۲۵ گروه، و رویداد ۱۰ فریمی دست‌نخورده ماند.

---

## ۱۰ · قدم بعد

1. نوت‌بوک `FireGuard_Dataset.ipynb` را در Colab باز کن و اجرا کن (~۳۰ دقیقه)
2. **سلول ۷ (بازبینی چشمی) را حتماً نگاه کن** — اگر کادرها سرجایشان بودند، نگاشت درست است
3. بعد از آن، نوت‌بوک آموزش

---

## منابع

- FASDD — https://www.tandfonline.com/doi/full/10.1080/10095020.2024.2347922 · آینه: https://huggingface.co/datasets/seawsurf/fire_smoke_dataset_fasdd_cv (CC BY 4.0)
- D-Fire — https://github.com/gaia-solutions-on-demand/DFireDataset · آینه: https://huggingface.co/datasets/badsaarow/d-fire
- Roboflow FASDD_CV — https://universe.roboflow.com/forestfiresmoke/fasdd_cv-dx83j/dataset/3


---

# دور دوم بازرسی — ۲۰۲۶-۰۹-۰۴

## ۱۱ · ★ خطای اندازه‌گیریِ خودم — اصلاح شد

در دور اول نوشتم FASDD «train ۷۲,۸۸۹ تصویر» دارد و ظاهراً **۳,۲۱۲ برچسبِ بی‌تصویر**.
هر دو غلط بود.

**علت:** فهرست مرکزیِ zip (central directory) ۲۵,۴۶۵,۹۵۹ بایت است ولی من فقط
۲۵,۰۰۰,۰۰۰ بایتِ آخرش را خوانده بودم. یعنی ۴۶۶ کیلوبایتِ ابتدایی ≈ **۳,۵۰۴ ورودی** جا مانده بود.

و دقیقاً همان عدد بود که به‌شکل «برچسبِ یتیم» ظاهر شد:
`۳,۲۱۲ + ۲۸۴ + ۸ = ۳,۵۰۴` — انطباق کامل.

بعد از خواندن آن ۴۶۶ کیلوبایتِ باقی‌مانده و پارس‌کردن هر ۱۹۰,۲۵۷ ورودی:

```
train  images  76,101  labels  76,101  orphan-labels 0  orphan-images 0
valid  images   9,513  labels   9,513  orphan-labels 0  orphan-images 0
test   images   9,512  labels   9,512  orphan-labels 0  orphan-images 0
TOTAL  images  95,126  labels  95,126
```

**هیچ ناهنجاری‌ای وجود نداشت. جفت‌شدن کامل ۱:۱ است.** و ۹۵,۱۲۶ دقیقاً همان عددی است
که `README.roboflow.txt` ادعا می‌کند. ✓

> درسِ عملی: هر ناهنجاریِ داده را قبل از باور کردن، اول به ابزارِ اندازه‌گیری شک کن.

---

## ۱۲ · یافتهٔ نو — تصاویر FASDD حداکثر ۶۴۰ پیکسل‌اند، **بدون نوارِ سیاه**

«Resize to 640x640 (Fit within)» در Roboflow یعنی *بلندترین ضلع = ۶۴۰، نسبت حفظ می‌شود* —
نه اینکه مربع شود با نوارِ padding.

اندازه‌گیری روی نمونه‌های واقعی:

| منبع | اندازه‌ها |
|---|---|
| FASDD | ۱۳ اندازهٔ متفاوت در ۲۲ نمونه: `640×360`، `640×480`، `360×640`، … |
| D-Fire | همه `1280×720` |

**پیامد عملی مهم:** بیش از ۸۰٪ داده حداکثر ۶۴۰ پیکسل است.
پس **`imgsz` را بالاتر از ۶۴۰ نبر** — روی FASDD صرفاً بزرگ‌نماییِ الکی است و
GPU و زمان را هدر می‌دهد بدون هیچ اطلاعات اضافه. (در نوت‌بوک آموزش نوشته شد.)

---

## ۱۳ · یافتهٔ نو — هندسهٔ برچسب‌ها تمیز است

روی ۹۶ کادر از هر دو دیتاست:

```
FASDD : {'oob': 0, 'zero': 0, 'cls': 0, 'fields': 0, 'dup': 0, 'empty': 4}
D-Fire: {'oob': 0, 'zero': 0, 'cls': 0, 'fields': 0, 'dup': 0, 'empty': 0}
```

صفر مختصاتِ بیرون از بازه، صفر کادرِ صفر، صفر کلاسِ نامعتبر، صفر خطِ بدفرمت، صفر کادرِ تکراری.
چهار «خالی» همان منفی‌های FASDD‌اند — فرمتِ درست.

**سهم اشیای کوچک: ۲۰.۸٪** از کادرها زیر ۳۲×۳۲ پیکسل در ۶۴۰ هستند.
همین عدد است که تشخیصِ زودهنگامِ دود را تعیین می‌کند — دلیل دیگری برای اینکه
`imgsz` را پایین‌تر از ۶۴۰ هم نبریم.

---

## ۱۴ · ★ تلهٔ نو ۳ — فریم‌های هم‌رویداد از تورِ حذف‌تکراری رد می‌شدند

**مشکل:** آستانهٔ حذف تکراری، فاصلهٔ همینگ ≤ ۳ بود. ولی فریم‌های متوالیِ AoF که
۸۰ تا ۹۰٪ شبیه‌اند، یعنی **۶ تا ۱۳ بیت اختلاف** — خیلی دورتر از ۳.

پس آن‌ها نه حذف می‌شدند (درست است، نباید حذف شوند) و نه با هم گروه می‌شدند —
مگر آنکه اسمشان در یک بازهٔ صدتایی می‌افتاد. رویدادی که از مرزِ صد رد می‌شد
(مثلاً `AoF04090` تا `AoF04110`) بین دو گروه و در نتیجه بین دو split پخش می‌شد.

**حل:** آستانهٔ دومی اضافه شد.

| فاصلهٔ همینگ | معنی | رفتار |
|---|---|---|
| ≤ ۳ | تکراری | یکی حذف می‌شود |
| **۴ تا ۱۲** | **هم‌رویداد** | **هر دو می‌مانند، ولی در یک split قفل می‌شوند** |
| > ۱۲ | مستقل | آزاد |

آزمون: `۲ جفت «هم‌رویداد» پیدا شد (فاصلهٔ 4..12) → در یک split قفل شدند` ·
`straddling >1 split: 0` ✓

---

## ۱۵ · ★ تلهٔ نو ۴ — الگوریتم حذف‌تکراری روی دادهٔ واقعی از کار می‌افتاد

کدِ دور اول سطل‌های بزرگ‌تر از ۴۰۰ عضو را **رد می‌کرد**:

```python
if len(idxs) < 2 or len(idxs) > 400: continue
```

**چرا خطرناک بود:** تصاویرِ تیره و یکنواخت (و ۳۹ هزار منفیِ FASDD پر از آسمان و دیوارِ
ساده‌اند) هزاران‌تایی **هشِ یکسان** می‌گیرند. آن‌ها یک سطلِ غول‌پیکر می‌سازند —
و دقیقاً همان سطل رد می‌شد. یعنی الگوریتم در پرتکرارترین حالت، هیچ تکراری‌ای پیدا نمی‌کرد.

**حل:** دو مرحله‌ای شد.

1. **گام ۱ — هشِ دقیقاً یکسان با dict، هزینهٔ O(n).** تمام تصاویرِ کاملاً یکسان
   (شاملِ همان خوشه‌های غول‌پیکر) اینجا و در زمان خطی گرفته می‌شوند.
2. **گام ۲ — نزدیک‌به‌هم، فقط روی هش‌های *یکتا*.** مجموعه بسیار کوچک‌تر است،
   پس سقف به ۶,۰۰۰ رفت و عملاً هرگز فعال نمی‌شود. اگر شد، **با پیام بلند گزارش می‌دهد**
   به‌جای اینکه بی‌صدا رد شود.

---

## ۱۶ · ★ یافتهٔ نو ۵ — قلّابِ رسمیِ Ultralytics برای augmentation

اولین نسخهٔ نوت‌بوکِ آموزش، `Albumentations.__init__` را monkey-patch می‌کرد. **دو باگ داشت:**

1. امضای واقعی `__init__(self, p=1.0, transforms=None, flip_idx=None)` است، ولی
   وصلهٔ من فقط `(self, p=1.0)` می‌گرفت → با فراخوانیِ واقعیِ Ultralytics می‌شکست.
2. وصله `label_fields=['class_labels']` می‌گذاشت، ولی Ultralytics
   `label_fields=["class_labels", "idx"]` استفاده می‌کند → کادرها خراب می‌شدند.

بعد در سورس پیدا شد که اصلاً نیازی به وصله نیست — `augment.py` خط ۲۸۴۹:

```python
Albumentations(p=1.0, transforms=getattr(hyp, "augmentations", None), flip_idx=flip_idx)
```

و `cfg/__init__.py` خط ۶۴۶:

```python
# Allow 'augmentations' as a valid custom parameter for custom Albumentations transforms
allowed_custom_keys = {"augmentations", "save_dir"}
```

یعنی کافی است `model.train(augmentations=[...])` بدهیم. تأیید شد:

```
>>> get_cfg(overrides={'augmentations': ['X']})
accepted; value = ['X']
```

**وصله کاملاً حذف شد** و جایش قلّابِ رسمی نشست — با به‌روزرسانی نسخه نمی‌شکند.

⚠️ نکته: دادنِ `transforms` مجموعهٔ پیش‌فرض را **جایگزین** می‌کند، پس موارد مفیدِ
پیش‌فرض در لیستِ ما دوباره آورده شده‌اند.

---

## ۱۷ · گروه‌بندی D-Fire — سالم است

از فهرستِ ۷,۳۹۳ فایلِ D-Fire که API برگرداند:

```
پیشوندها: WEB 4,040 · AoF 2,912 · PublicDataset 441
قاعدهٔ فعلی → 216 گروه
بزرگ‌ترین گروه: 52 تصویر = ۰.۷٪ از D-Fire
ده گروه بزرگ روی هم: 506 = ۶.۸٪
```

هیچ گروهِ غول‌پیکری وجود ندارد که یک‌تنه یک split را منحرف کند. ✓

---

## ۱۸ · وضعیت workflow موازی

یک workflow با ۱۴ عاملِ موازی برای بازرسی عمیق‌تر فرستادم؛ **هر ۱۴ عامل به سقفِ
مصرفِ جلسه خوردند و هیچ نتیجه‌ای برنگرداندند.** بنابراین یافته‌های بخش‌های ۱۱ تا ۱۷
همگی حاصل بررسیِ مستقیمِ خودم است، نه آن workflow.
