# داده و مدل — بررسی دقیق پیش از آموزش

تاریخ: ۲۰۲۶-۰۹-۰۳ · مکمل [PLAN_V2.md](PLAN_V2.md)

---

## ★ یافتهٔ مهم — و اصلاحِ توصیهٔ قبلیِ من

ویدیوهای `فیلم های پروژه بهینه شده/آتش/` را باز کردم و فریم استخراج کردم. آنچه دیدم:

| ویدیو | صحنه |
|---|---|
| `fire.mp4` | **دوربین مداربستهٔ خانگی** — نشیمن، برچسب `CAMERA02` و تاریخ روی تصویر |
| `fire_1.mp4` | مغازه/بازار، آتش‌گرفتن در فضای باز شهری، نمای نزدیک |
| `fire_2.mp4` | نشیمنِ خانه، دوربین **Nest**، شعله از سمت چپ |
| `fire_3.mp4` | نشیمنِ خانه، کلیپ خبری «قطرات مذاب» |
| `fire_4.mp4` | نشیمن با **درخت کریسمس** در حال آتش‌گرفتن |
| `fire_5.mp4` | **دفتر کار** — کمد بایگانی، پله، دوربین `Camera Lockers`، مهر تاریخ `01:29:14 AM` |
| `fire_8.mp4` | راهروی خانه، مردی درِ همسایه را آتش می‌زند |
| `2.mp4` | **دفتر کار** با میزها، دوربین `IP Camera 01`، لنز واید/فیش‌آی |
| `5.mp4` | نشیمن پر از **دود غلیظ** |

**نتیجه: دامنهٔ تو «آتش‌سوزی جنگل» نیست — «دوربین مداربستهٔ داخلی و شهری» است.**
خانه، دفتر، مغازه. لنز واید، رزولوشن پایین (۳۲۰×۲۴۰ تا ۱۲۸۰×۷۲۰)، فشرده‌سازی سنگین،
مهر تاریخ روی تصویر، و برخی در شب.

### این یعنی توصیهٔ قبلی من در PLAN_V2 اشتباه بود

در پلن، **Pyro-SDIS** را دیتاست اصلی معرفی کردم. آن دیتاست تصویرِ **دودِ دوردستِ جنگل از
بالای برج دیده‌بانی** است — ستون دودی که ۵ کیلومتر آن‌طرف‌تر بالای درختان است.

هیچ ربطی به شعله‌ای که در نشیمن از مبل بالا می‌رود ندارد. **از لیست اصلی حذفش می‌کنم.**

| دیتاست | برای دامنهٔ تو |
|---|---|
| ~~Pyro-SDIS~~ | ❌ برج جنگلی، دودِ دوردست — دامنهٔ اشتباه |
| ~~FIgLib~~ | ❌ برج، جنوب کالیفرنیا — دامنهٔ اشتباه |
| ~~FLAME~~ | ❌ پهپاد، جنگل کاج آریزونا — دامنهٔ اشتباه |
| **FASDD_CV** | ✅✅✅ **اصلی** — صراحتاً «داخلی و بیرونی، روز و شب، دوربین نظارتی» |
| **D-Fire** | ✅✅ مخلوط، و ۹,۸۳۸ منفیِ سخت که طلاست |
| **Indoor FS** | ✅✅ کوچک ولی دقیقاً همین دامنه (شامل فوتیج CCTV) |

---

# بخش ۱ — با کدام مدل آموزش بدهیم؟

## حدست درست بود: **YOLO26** ✅

| | YOLOv8 (۲۰۲۳) | YOLO11 (۲۰۲۴) | **YOLO26 (ژانویه ۲۰۲۶)** |
|---|---|---|---|
| AP کوکو (n→l) | پایه | +۱ تا ۲ | **+۱.۶ تا ۲.۸ نسبت به YOLO11** |
| NMS | لازم | لازم | ❌ **NMS-free (سرتاسری)** |
| DFL | دارد | دارد | ❌ حذف شده |
| بهینه‌ساز | SGD/Adam | SGD/Adam | **MuSGD** |
| تخصیص برچسب | TAL | TAL | **STAL** |
| اشیای کوچک | متوسط | خوب | **بهتر** ← مهم برای دودِ اولیه |
| ONNX روی CPU | پایه | پایه | **تا ۴۳٪ سریع‌تر از YOLO11n** |

### چهار دلیل که چرا YOLO26 برای *این* پروژه:

1. **NMS-free مستقیماً به هدفِ چند-دوربینی کمک می‌کند.** NMS روی CPU اجرا می‌شود، batch نمی‌شود، و تأخیرش با تعداد کادرها بالا می‌رود. حذفش یعنی تأخیرِ **قابل پیش‌بینی** — چیزی که وقتی ۸ دوربین روی یک GPU داری حیاتی است.
2. **اشیای کوچک بهتر** = دودِ اولیه زودتر دیده می‌شود = TTD پایین‌تر. این دقیقاً معیارِ فروش است.
3. **همان API نسخه‌های قبل.** هر آموزشی که برای YOLOv8 نوشته شده، با عوض‌کردن یک رشته کار می‌کند.
4. **CPU سریع‌تر** = مدل SPARK روی سخت‌افزار ارزان.

**سایزها:** آموزش اصلی را با `yolo26s` بزن (تعادل). بعد `yolo26n` برای SPARK و
`yolo26m/l` برای INFERNO. **دستور پخت یکی است، فقط اسم مدل عوض می‌شود.**

⚠️ **اما یک چیز را بدان:** Ultralytics لایسنس **AGPL-3.0** دارد و ادعا می‌کند وزن‌های
fine-tune شده هم اثرِ مشتق‌اند. یعنی وزنی که با YOLO26 می‌سازی، برای فروشِ بسته
مشکل‌دار است.

**پیشنهاد عملی من — «دو مرحله‌ای»:**

```
مرحلهٔ ۱ (الان): با YOLO26 آموزش بده
   ← سریع‌ترین راه به عددِ واقعی. دستور پخت را کشف می‌کنیم:
     چه داده‌ای، چه augmentation، چند epoch، چه آستانه‌ای

مرحلهٔ ۲ (وقتی خریدار داشتی): همان دستور پخت روی D-FINE-N (Apache-2.0)
   ← ~۶ ساعت GPU. چون دستور پخت از قبل کشف شده، ریسک صفر است.
```

**۹۰٪ ارزشِ کار در «دستور پخت» است، نه در معماری.** و دستور پخت کاملاً منتقل می‌شود.
پس با YOLO26 شروع کن — وقت را ذخیره می‌کنی بدون اینکه مسیر تجاری را ببندی.

---

# بخش ۲ — بله، شبکهٔ عصبی است. دقیقاً چه اتفاقی می‌افتد؟

## ۲-۱ YOLO26 از سه قسمت ساخته شده

```
تصویر ۶۴۰×۶۴۰×۳
      │
      ▼
┌─────────────────┐
│    BACKBONE     │  ~۱۰ تا ۲۰ لایهٔ کانولوشن
│  (ستون فقرات)   │  لایه‌های اول: لبه، گوشه، لکهٔ رنگی
└────────┬────────┘  لایه‌های آخر: بافت، الگو، شکلِ نیمه‌معنادار
         │
         ▼           سه مقیاس: 80×80 (ریز) · 40×40 (متوسط) · 20×20 (درشت)
┌─────────────────┐
│      NECK       │  ادغام مقیاس‌ها (FPN/PAN)
│     (گردن)      │  → شیء کوچک از لایهٔ ریز، شیء بزرگ از لایهٔ درشت
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│      HEAD       │  برای هر نقطه: (x, y, w, h) + امتیازِ هر کلاس
│      (سر)       │  در YOLO26: خروجی مستقیم، بدون NMS
└─────────────────┘
```

مجموعاً حدود **۹.۵ میلیون عدد** (وزن) در `yolo26s`. آموزش یعنی **پیدا کردن این ۹.۵ میلیون عدد**.

## ۲-۲ آموزش از صفر نمی‌دهیم — **Fine-tuning** می‌کنیم

این مهم‌ترین نکتهٔ عملی است:

| | آموزش از صفر | **Fine-tuning (کاری که ما می‌کنیم)** |
|---|---|---|
| وزن اولیه | تصادفی | **وزن‌های COCO** (۱۱۸ هزار تصویر، ۸۰ کلاس) |
| داده لازم | میلیون‌ها تصویر | ۲۰ تا ۱۰۰ هزار کافی است |
| زمان روی T4 | هفته‌ها | **۵ تا ۸ ساعت** |
| نتیجه | بدتر | بهتر |

**چرا کار می‌کند؟** لایه‌های اولِ شبکه‌ای که روی COCO آموزش دیده، «لبه»، «گرادیان رنگ»،
«بافتِ آشفته» را یاد گرفته‌اند. اینها **جهانی**اند — برای گربه و ماشین و شعله یکی‌اند.
فقط لایه‌های آخر باید یاد بگیرند که «این الگوی خاص = آتش».

پس ما شبکه را از COCO برمی‌داریم، سرش را برای ۲ کلاس (`fire`, `smoke`) عوض می‌کنیم،
و با نرخ یادگیریِ **پایین** ادامه می‌دهیم تا وزن‌های خوبِ قبلی خراب نشوند.

## ۲-۳ حلقهٔ آموزش — دقیقاً چه می‌گذرد

```
تکرار برای هر epoch (عبورِ کامل روی همهٔ داده‌ها):
   تکرار برای هر batch (مثلاً ۱۶ تصویر):

     ۱. augmentation  → تصاویر را عمداً خراب می‌کنیم:
                        چرخش، برش، تغییر رنگ، mosaic (۴ تصویر در ۱)
                        هدف: شبکه به شرایطِ ندیده مقاوم شود

     ۲. forward       → شبکه پیش‌بینی می‌کند: «آتش، اینجا، ۰.۷۳»

     ۳. loss          → مقایسه با برچسبِ درست:
                        · خطای مکان (کادر کجاست)  ← CIoU
                        · خطای کلاس (آتش یا دود)   ← BCE
                        · تخصیص (کدام نقطه مسئولِ کدام شیء) ← STAL

     ۴. backward      → مشتقِ خطا نسبت به هر یک از ۹.۵ میلیون وزن

     ۵. optimizer     → هر وزن را کمی در جهتِ کاهش خطا حرکت می‌دهد (MuSGD)
```

بعد از ~۱۰۰ epoch، خطا دیگر کم نمی‌شود → مدل همگرا شده.

## ۲-۴ کدِ واقعیِ Colab

```python
# ---------- ۱. نصب ----------
!pip install -q ultralytics

# ---------- ۲. فایل تعریف داده ----------
%%writefile fire.yaml
path: /content/dataset
train: images/train
val:   images/val
names:
  0: fire
  1: smoke

# ---------- ۳. آموزش ----------
from ultralytics import YOLO

model = YOLO("yolo26s.pt")          # ← وزن COCO، نه از صفر

model.train(
    data      = "fire.yaml",
    epochs    = 120,
    imgsz     = 640,
    batch     = 16,                 # T4 با 16GB: 16 برای s، 32 برای n
    device    = 0,
    workers   = 2,                  # Colab بیشتر از ۲ گیر می‌کند

    # --- نرخ یادگیری ---
    optimizer = "auto",             # YOLO26 خودش MuSGD می‌گذارد
    lr0       = 0.001,              # پایین، چون fine-tune است نه از صفر
    lrf       = 0.01,               # نرخ نهایی = lr0 × lrf
    warmup_epochs = 3,
    cos_lr    = True,

    # --- augmentation: اینجا جای خلاقیت است ---
    hsv_h     = 0.015,              # رنگ را کم دست بزن — رنگ آتش معنادار است
    hsv_s     = 0.7,
    hsv_v     = 0.4,                # روشنایی زیاد → مقاوم به شب و نورِ کم
    degrees   = 5.0,                # دوربین سقفی کمی کج است
    translate = 0.1,
    scale     = 0.5,                # آتش دور و نزدیک
    fliplr    = 0.5,
    mosaic    = 1.0,                # ۴ تصویر در ۱ → context بیشتر
    close_mosaic = 15,              # ۱۵ epoch آخر بدون mosaic (تثبیت)
    mixup     = 0.1,
    erasing   = 0.2,                # انسداد جزئی

    # --- ذخیره و ادامه ---
    project   = "/content/drive/MyDrive/fireguard_runs",
    name      = "yolo26s_v1",
    save_period = 5,                # ★ هر ۵ epoch چک‌پوینت → Colab قطع شد، ادامه بده
    patience  = 30,
)

# ---------- ۴. ادامه بعد از قطعیِ Colab ----------
# model = YOLO("/content/drive/MyDrive/fireguard_runs/yolo26s_v1/weights/last.pt")
# model.train(resume=True)

# ---------- ۵. ارزیابی ----------
metrics = model.val(data="fire.yaml", split="test", conf=0.001, iou=0.6)
print("mAP@50    :", metrics.box.map50)
print("mAP@50-95 :", metrics.box.map)
print("per class :", metrics.box.maps)     # [fire, smoke] جدا
```

### دو نکتهٔ ریز که فرق می‌گذارند

**`freeze` نزن.** وسوسه می‌شوی backbone را فریز کنی تا سریع‌تر شود. نکن —
بافتِ شعله و دود آن‌قدر از اشیای COCO دور است که backbone باید تطبیق پیدا کند.
فریزکردن ۳ تا ۵ واحد mAP می‌سوزاند.

**`hsv_h` را کم بگذار.** در بیشتر مسائل، تغییر تُنِ رنگ augmentation خوبی است.
اینجا نه — **رنگِ نارنجی-قرمز خودش نصفِ سیگنالِ آتش است.** اگر زیاد جابه‌جایش کنی،
به شبکه یاد می‌دهی رنگ را نادیده بگیرد.

---

# بخش ۳ — مشخص‌کردن دقیقِ محل آتش

> ## ❌ این بخش کنسل شد — ۲۰۲۶-۰۹-۰۴
>
> تصمیم گرفته شد **کادر مستطیلی کافی است**؛ قطعه‌بندی پیکسلی و ماسک‌سازی با SAM 2
> اجرا نمی‌شود. بخش زیر فقط به‌عنوان سابقه نگه داشته شده.
>
> پیامد: «نرخ گسترش بر حسب متر مربع» در INFERNO هم فعلاً از دستور کار خارج است.
> اگر بعداً لازم شد، همین نقشه سرجایش هست.

## ۳-۱ کادر (bbox) برای «محلِ دقیق» کافی نیست

شعله شکلِ مستطیل ندارد. یک کادرِ دورِ شعله معمولاً **۴۰ تا ۶۰ درصدش پس‌زمینه است**.

| سطح | چه می‌دهد | مدل |
|---|---|---|
| **کادر** (الان) | مستطیل | `yolo26s` |
| **★ قطعه‌بندی نمونه‌ای** | **ماسکِ پیکسل‌به‌پیکسل** | `yolo26s-seg` |
| نقطه‌ای | فقط مرکز | — |

قطعه‌بندی چهار چیز می‌دهد که کادر نمی‌دهد:

1. **مرزِ واقعیِ آتش** — ناحیهٔ ایمنیِ خط‌چینِ تو دیگر حدس نیست
2. **مساحتِ واقعی به پیکسل** → با کالیبراسیون دوربین، **متر مربع**
3. **نرخِ گسترش** = مشتقِ زمانیِ مساحت → «۱.۴ متر مربع در دقیقه، به سمت راهروی خروج»
4. **دقتِ بالاتر در خودِ تشخیص** — ماسک نمی‌گذارد شبکه با پس‌زمینه تقلب کند

بندهای ۲ و ۳ همان چیزی است که در PLAN_V2 به‌عنوان قابلیتِ **INFERNO** نوشتم.
یعنی: **قطعه‌بندی، پیش‌نیازِ «هوشِ موقعیتی» است، نه یک تزئین.**

## ۳-۲ مشکل: هیچ دیتاست بزرگی ماسک ندارد

| دیتاست ماسک‌دار | تعداد | محیط |
|---|---|---|
| Corsican Fire DB | ۶۴۰ جفت (RGB + مادون‌قرمز نزدیک) | آتشِ جنگل، نمای نزدیک |
| FLAME | ۲,۰۰۳ تصویرِ ماسک‌خورده | پهپاد، جنگل کاج |
| BoWFire | ۱۱۹ تصویر آتش با ماسک | مخلوط داخلی/بیرونی |
| **جمع** | **~۲,۷۰۰** | و هیچ‌کدام دودِ داخلی ندارند |

۲,۷۰۰ تصویر برای آموزشِ یک مدلِ قطعه‌بندیِ خوب **کافی نیست**.

## ۳-۳ ★ راه‌حل: ماسک‌سازیِ خودکار با SAM 2

D-Fire و FASDD روی هم **بیش از ۱۱۰ هزار کادرِ آتش و ۷۵ هزار کادرِ دود** دارند.
کادرها هستند، ماسک‌ها نیستند. اما SAM 2 دقیقاً برای همین ساخته شده:

```
برای هر کادرِ برچسب‌خوردهٔ موجود:
        │
        ├─ کادر را به‌عنوان prompt به SAM 2 بده
        │       (SAM 2 از box prompt پشتیبانی می‌کند)
        │
        ├─ ماسکِ پیکسلی بگیر
        │
        ├─ فیلترِ کیفیت:
        │     · ماسک باید ≥۳۰٪ کادر را پر کند (نه یک تکهٔ نازک)
        │     · ماسک نباید از کادر بیرون بزند
        │     · برای آتش: هم‌خوانی با نقشهٔ کرومایِ شعله بررسی شود
        │
        └─ ماسکِ قبول‌شده = برچسبِ کاذب (pseudo-label)
```

چرا اینجا خوب جواب می‌دهد: **شعله کنتراستِ بالا و مرزِ روشن دارد** — دقیقاً آسان‌ترین
حالت برای SAM. (دود سخت‌تر است؛ برای دود آستانهٔ سخت‌گیرانه‌تر می‌گذاریم.)

**اعتبارسنجی:** مدلِ قطعه‌بندیِ حاصل را روی Corsican و BoWFire — که **ماسکِ واقعیِ
دستِ انسان** دارند — می‌سنجیم. اگر IoU ماسک ≥ ۰.۷۵ شد، برچسب‌های کاذب معتبرند.

هزینه: چند ساعت GPU، **صفر ساعت برچسب‌زنیِ دستی**.
نتیجه: از ۲,۷۰۰ ماسک به **۱۰۰ هزار+ ماسک** می‌رسیم.

> این یکی از آن نوآوری‌هایی است که در ارائه خوب می‌نشیند:
> «ما دیتاستِ قطعه‌بندیِ آتش را نساختیم — گذاشتیم مدل بسازد.»

---

# بخش ۴ — چه چیزهایی روی GitHub هست؟

## ۴-۱ آنچه بررسی کردم

| ریپو | چیست | ارزش برای ما |
|---|---|---|
| **`pedbrgs/Fire-Detection`** | YOLOv5 + **AVT** (تکنیک تغییرِ مساحت) و **TPT** (پایداریِ زمانی). مقاله در *Neural Computing and Applications* ۲۰۲۳. **لایسنس MIT.** از همان گروهی که D-Fire را ساخت (UFMG برزیل) | ★★★ **بخوانش.** AVT دقیقاً همان «تأییدکنندهٔ رشدِ ستون» من است — یعنی ایده‌ام داورِ همتا خورده. TPT هم نسخهٔ سادهٔ تأییدِ زمانی. MIT یعنی می‌توانیم بسازیم رویش. |
| `spacewalk01/yolov5-fire-detection` | آموزش YOLOv5/v9 روی ویدیو | ★ فقط برای مقایسه |
| `CostiCatargiu/NEWFireSmokeDataset` | نتایج تجربیِ YOLO5/6/7/8/9/10/NAS روی یک دیتاست | ★★ جدولِ مقایسه‌اش مفید است |
| `robmarkcole/fire-detection-from-images` | فهرستِ منابع و دیتاست‌ها | ★★ نقشهٔ راهِ خوبی برای پیداکردن دیتاست |
| `Abonia1`, `luminous0219`, `salim4n` | fine-tuneهای YOLOv8، بدون متریکِ منتشرشده | ✗ همان مشکلِ چک‌پوینت‌های فعلی — کیفیتِ نامعلوم |
| **`obss/SAHI`** | استنتاج کاشی‌ای | ★★★ مستقیم در INFERNO استفاده می‌شود |

## ۴-۲ جوابِ سؤالِ «آماده استفاده کنیم یا مالِ خودمان؟»

پاسخ سه‌بخشی است — نه «یا این یا آن»:

| لایه | تصمیم | چرا |
|---|---|---|
| **معماری** | آماده (YOLO26 / D-FINE) | معماریِ نو ساختن، کارِ یک تیمِ تحقیقاتی با ۱۰۰ GPU است. ما هرگز از آنها جلو نمی‌زنیم و لازم هم نیست. |
| **وزن‌ها** | ★ **مالِ خودمان** | اینجا مالکیت ایجاد می‌شود. وزنی که خودت روی دادهٔ شناخته‌شده آموزش دادی، قابلِ اندازه‌گیری، قابلِ دفاع، و قابلِ فروش است. |
| **منطقِ بالای مدل** | ★★★ **مالِ خودمان** | سوسوی DFT، رشدِ ستون، باسِ log-odds، قاضیِ VLM، حقِ سکوت. **هیچ‌کدام روی GitHub نیست.** تمایزِ واقعیِ محصول اینجاست. |

> کسی برای «ما YOLO26 را صدا زدیم» پول نمی‌دهد.
> برای «سیستمِ ما فهمید آن نورِ نارنجی، غروبِ آفتاب است، چون در ۲ تا ۱۲ هرتز سوسو نمی‌زد» پول می‌دهد.

---

# بخش ۵ — داده‌ها: تحلیل دقیق

## ۵-۱ جدولِ کاملِ دیتاست‌ها

| دیتاست | تعداد | ترکیب | برچسب | فرمت | لایسنس | تناسب با دامنهٔ تو |
|---|---|---|---|---|---|---|
| **FASDD** (کل) | ۱۲۲,۶۳۴ | ۷۰,۵۸۱ مثبت / **۵۲,۰۷۳ منفی**؛ ۱۱۳,۱۵۴ نمونهٔ شعله، ۷۳,۰۷۲ نمونهٔ دود | کادر | **YOLO + VOC + COCO + TDML** | **CC BY 4.0** ✅ تجاری | — |
| ↳ **FASDD_CV** | **~۹۵,۳۱۴** | زیرمجموعهٔ دوربینی | کادر | همان | همان | ✅✅✅ **اصلی** |
| ↳ FASDD_UAV | ~۲۱,۰۰۰ | پهپاد | کادر | همان | همان | ✗ |
| ↳ FASDD_RS | ۵,۷۷۳ | ماهواره | کادر | همان | همان | ✗ |
| **D-Fire** | ۲۱,۵۲۷ | فقط آتش ۱,۱۶۴ · فقط دود ۵,۸۶۷ · هردو ۴,۶۵۸ · **هیچ‌کدام ۹,۸۳۸** | کادر | YOLO (نرمال‌شده) | آزاد | ✅✅ منفی‌هایش طلاست |
| **Indoor FS** | ۵,۰۰۰ | داخلی، شامل CCTV؛ تقسیم ۸۰/۱۰/۱۰ | کادر | — | بررسی شود | ✅✅ دقیقاً دامنهٔ تو |
| Corsican Fire DB | ۶۴۰ جفت | RGB + NIR، **ماسکِ دستِ متخصص** | **ماسک** | — | پژوهشی | ✅ فقط اعتبارسنجیِ قطعه‌بندی |
| BoWFire | ۲۲۶ (۱۱۹ ماسک) | مخلوط | **ماسک** | — | پژوهشی | ✅ فقط اعتبارسنجی |
| FLAME | ۲,۰۰۳ | پهپاد، جنگل کاج آریزونا | **ماسک** | — | پژوهشی | ✗ دامنهٔ اشتباه |
| Pyro-SDIS | ۳۳,۶۳۶ | ۲۸,۱۰۳ دودی، **فقط ۱ کلاس: دود** | کادر | YOLO | Apache-2.0 | ✗ برجِ جنگلی |
| FIgLib | ~۲۰,۰۰۰ | دنبالهٔ زمانی، برجِ کالیفرنیا | کادر | — | پژوهشی | ✗ ولی برای سرِ زمانی مفید |
| DetectiumFire | ۱۴.۵k + ۲.۵k ویدیو | کپشن + سطحِ خطر | کادر + متن | — | ❌ **CC BY-NC-SA** | فقط ارزیابی، **هرگز آموزش** |

## ۵-۲ آیا برچسب‌ها دقیق‌اند؟ — بررسیِ صادقانه

| دیتاست | کیفیتِ برچسب | چه می‌دانیم |
|---|---|---|
| **FASDD** | ★★★ | مقالهٔ داوری‌شده (Geo-spatial Information Science، ۲۰۲۴)، چهار فرمتِ خروجی، تقسیمِ رسمیِ train/val/test با نسبتِ ۱/۲، ۱/۳، ۱/۶ |
| **D-Fire** | ★★★ | مقاله در Multimedia Tools and Applications، از UFMG. **کلاسِ `none` عمداً با نمونه‌های گمراه‌کننده پر شده**: نورِ لامپ، تابشِ خورشید، ابرِ شبیهِ دود، غروبِ سرخ |
| Indoor FS | ★★ | مقاله دارد، ولی جزئیاتِ فرایندِ برچسب‌زنی منتشر نشده |
| Corsican | ★★★ | **ماسکِ دستیِ متخصص** — بالاترین کیفیتِ موجود |
| Pyro-SDIS | ★★ | برچسبِ داوطلبانِ انجمن Pyronear، بدون داورِ دوم |

**نکتهٔ مهم دربارهٔ کادرِ دود:** دود مرزِ مشخص ندارد. حتی دو انسانِ متخصص هم برای یک
ستونِ دود کادرِ یکسان نمی‌کشند. پس **mAP دود ذاتاً از mAP آتش پایین‌تر است** —
این عیبِ مدلِ تو نیست، عیبِ خودِ مسئله است. در گزارش‌ها باید جدا اعلامشان کنی.

## ۵-۳ محیط‌های پوشش‌داده‌شده در FASDD_CV و D-Fire

FASDD صراحتاً می‌گوید تنوعش شامل این محورهاست:

| محور | مقادیر |
|---|---|
| فاصله | نزدیک ↔ دور |
| صحنه | **داخلی ↔ بیرونی** ✅ |
| نور | **روز ↔ شب** ✅ |
| زاویه | نمای بالا ↔ نمای جانبی |
| حسگر | **دوربین نظارتی** ✅ · پهپاد · ماهواره |
| منبع | اینترنت · شبکه‌های اجتماعی · دیتاست‌های باز |

D-Fire هم مخلوط است اما وزنش به بیرونی/جنگلی می‌چربد. **ارزشِ اصلیِ D-Fire برای ما،
۹,۸۳۸ تصویرِ منفیِ سختش است** — این تنها مجموعهٔ منفیِ *عمداً گمراه‌کننده* است که وجود دارد.

## ۵-۴ آیا کافی است؟ — **بله، برای آموزش. نه، برای ادعای FAR.**

### آنچه داریم (بعد از ادغام)

```
FASDD_CV   ~۹۵,۳۱۴  ┐
D-Fire      ۲۱,۵۲۷  ├─►  ~۱۲۱,۸۴۱ تصویر
Indoor FS    ۵,۰۰۰  ┘
                          ~۱۱۳,۰۰۰ نمونهٔ شعله
                          ~۷۵,۰۰۰  نمونهٔ دود
                          ~۶۲,۰۰۰  تصویرِ منفی
```

**برای مقایسه: کلِ COCO ۱۱۸ هزار تصویر برای ۸۰ کلاس دارد.**
ما ~۱۲۲ هزار تصویر برای **۲ کلاس** داریم. این نه‌تنها کافی است — **فراوان است.**

نسبتِ منفی به مثبت هم عالی است (~۱:۲). این دقیقاً چیزی است که FAR پایین می‌آورد.

### سه شکافِ واقعی که باقی می‌ماند

| شکاف | چرا مهم است | راه‌حل |
|---|---|---|
| **۱ · حالتِ شبانهٔ مادون‌قرمز** | دوربین مداربسته شب به IR سوئیچ می‌کند: **خاکستری، بدون رنگ**. آتش در این حالت یک لکهٔ سفید است. دیتاست‌های عمومی «شب رنگی» دارند نه «شبِ IR». **و کانالِ رنگیِ TC3 من در این حالت می‌میرد.** | (الف) augmentation: تبدیل ۱۵٪ داده به خاکستری + گامای IR ← رایگان<br>(ب) TC3 باید حالتِ IR را تشخیص دهد و به کانالِ زمانی وزنِ بیشتر بدهد<br>(ج) اگر فوتیجِ شبانه گیر آوردی، طلاست |
| **۲ · هندسهٔ دوربینِ تو** | لنزِ واید/فیش‌آی سقفی، ۳۲۰×۲۴۰ تا ۷۲۰p، فشرده‌سازی سنگین، مهر تاریخ روی تصویر | augmentation: افتِ رزولوشن + نویزِ JPEG + اعوجاجِ بشکه‌ای. **این را حتماً اضافه کن** — بدونش مدل روی تصویرِ تمیزِ اینترنتی خوب و روی CCTV بد می‌شود |
| **۳ · سنجشِ FAR** | هیچ دیتاستِ عمومی نمی‌تواند بگوید «شبی چند بار الکی زنگ می‌زند» | **فقط ویدیوی خامِ خودت** — بدون برچسب |

## ۵-۵ پس آیا باید داده جمع کنم؟

| کار | نظر من | چرا |
|---|---|---|
| برچسب‌زدنِ دستیِ تصاویرِ آتش | ❌ **نه** | ۱۲۲ هزار تصویرِ برچسب‌خورده از قبل داری. یک هفته کارِ دستی، ۰.۵٪ به داده اضافه می‌کند. |
| دانلود از Roboflow Universe | ⚠️ **با احتیاط** | بیشترشان همان D-Fire و FASDD‌اند که دوباره بسته‌بندی شده‌اند. خطر: **همان تصویر در train و test هر دو بیفتد** → عددِ دروغینِ بالا. اگر برداشتی، حتماً هش‌گیری و حذفِ تکراری کن. |
| ★ **ضبطِ ویدیوی خامِ صحنه‌های عادی** | ✅ **بله، همین حالا** | صفر ساعت برچسب‌زنی. تنها راهِ سنجشِ FAR. و در فاز ۵ خودِ INFERNO برچسبش می‌زند. |
| ★ **ضبط در حالتِ شبانهٔ IR** | ✅ **بله** | تنها شکافی که augmentation کاملاً پرش نمی‌کند |

### چه چیزی ضبط کنی (فقط ضبط — برچسب نه)

| صحنه | چرا | مدت |
|---|---|---|
| صحنهٔ عادیِ روز، بدون آتش | خطِ پایهٔ FAR | ۴ ساعت |
| **غروبِ آفتاب از پنجره** | سخت‌ترین منفی | ۱ ساعت |
| **حالتِ شبانهٔ IR** | شکافِ شمارهٔ ۱ | ۴ ساعت |
| بخارِ کتری / آشپزخانه | شبیه‌ترین چیز به دود | ۳۰ دقیقه |
| چراغِ چشمک‌زن، تلویزیونِ روشن | منفیِ سوسودار — آزمونِ تأییدکنندهٔ DFT | ۳۰ دقیقه |
| رفت‌وآمدِ عادیِ آدم‌ها | حرکت بدون آتش → آزمونِ زمان‌بندِ دروازه‌ای | ۲ ساعت |

**جمعاً ~۱۲ ساعت ضبط، صفر ساعت برچسب.** این کلِ کاری است که باید بکنی.

---

# بخش ۶ — جمع‌بندیِ تصمیم‌ها

| سؤال | تصمیم |
|---|---|
| کدام مدل؟ | **YOLO26** (`yolo26s` برای آموزشِ اصلی) — حدست درست بود. بعداً همان دستور پخت روی D-FINE برای مسیرِ تجاری. |
| شبکهٔ عصبی؟ | بله — CNN عمیق، ~۹.۵M وزن. **Fine-tune از وزنِ COCO**، نه آموزش از صفر. ۵ تا ۸ ساعت روی T4. |
| محلِ دقیق؟ | **قطعه‌بندی** (`yolo26s-seg`)، با ماسک‌های تولیدشده توسط **SAM 2** از کادرهای موجود |
| مدلِ آماده یا خودمان؟ | معماری: آماده · **وزن: خودمان** · **منطقِ بالای مدل: خودمان** ← تمایز اینجاست |
| کدام داده؟ | **FASDD_CV + D-Fire + Indoor FS** ≈ ۱۲۲ هزار تصویر. ~~Pyro-SDIS~~ حذف شد (دامنهٔ اشتباه). |
| برچسب‌ها دقیق‌اند؟ | بله، FASDD و D-Fire هر دو مقالهٔ داوری‌شده دارند و فرمتِ YOLO آماده. کادرِ دود ذاتاً مبهم است — جدا گزارش شود. |
| کافی است؟ | **برای آموزش: بله، فراوان.** برای ادعای FAR: نه — ~۱۲ ساعت ویدیوی خامِ خودت لازم است. |
| داده جمع کنم؟ | برچسب‌زنی ❌ · **ضبطِ ویدیوی خام ✅** |

---

## منابع

- FASDD — https://essd.copernicus.org/preprints/essd-2023-73/ · https://doi.org/10.57760/sciencedb.j00104.00103 · https://www.tandfonline.com/doi/full/10.1080/10095020.2024.2347922
- D-Fire — https://github.com/gaia-solutions-on-demand/DFireDataset
- Indoor fire YOLOv5 — https://journals.plos.org/plosone/article?id=10.1371%2Fjournal.pone.0322052
- CCTV early fire detection — https://www.sciencedirect.com/science/article/abs/pii/S2352710222016539
- YOLO26 — https://docs.ultralytics.com/models/yolo26 · https://arxiv.org/html/2606.03748v1
- D-FINE — https://github.com/Peterande/D-FINE
- Fire-Detection (AVT/TPT، MIT) — https://github.com/pedbrgs/Fire-Detection
- SAHI — https://github.com/obss/SAHI
- دیتاست‌های قطعه‌بندی — https://www.nature.com/articles/s41598-025-01571-5
- Pyro-SDIS (حذف‌شده از لیستِ اصلی) — https://huggingface.co/datasets/pyronear/pyro-sdis
